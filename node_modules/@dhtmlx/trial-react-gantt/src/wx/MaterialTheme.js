import React, { Component } from "react";
import "./MaterialTheme.css";

export default class MaterialTheme extends Component {
  render() {
    return this.props.children ? (
      <div className="wx-material" style={{ height: "100%" }}>
        {this.props.children}
      </div>
    ) : null;
  }
}
