import React, { Component } from "react";
import "./DefaultTheme.css";

export default class DefaultTheme extends Component {
  render() {
    return this.props.children ? (
      <div className="wx-default" style={{ height: "100%" }}>
        {this.props.children}
      </div>
    ) : null;
  }
}
